import * as React from 'react';
import SingleSpaceContainer from 'front.core/lib/modules/spaces/containers/singleSpace.container';
import { SingleSpace } from '../components/spaces/SingleSpace';

export const SingleSpaceView = () => (
  <SingleSpaceContainer>
    <SingleSpace />
  </SingleSpaceContainer>
);
